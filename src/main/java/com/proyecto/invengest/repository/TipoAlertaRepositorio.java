package com.proyecto.invengest.repository;

import com.proyecto.invengest.entities.TipoAlerta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoAlertaRepositorio extends JpaRepository<TipoAlerta, Integer> {
}
